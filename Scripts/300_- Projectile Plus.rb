#===============================================================================
# * Projectile
#-------------------------------------------------------------------------------
#   The original script is pitty, so the add-ons are here.
#===============================================================================
class Projectile < Game_Character
  
  #----------------------------------------------------------------------------
  # *) Find path to the target
  #----------------------------------------------------------------------------
  def find_path_to_target
    
  end
  #----------------------------------------------------------------------------
  # *) Assign pre-move positions
  #----------------------------------------------------------------------------
  def assign_premove_positions(positions, forced = false)
    @move_positions = positions
  end
  #----------------------------------------------------------------------------
  # *) update tool movement
  #----------------------------------------------------------------------------
  alias update_movement_abs update_tool_movement
  def update_tool_movement
    
    update_movement_abs if @move_positions.empty?
  end
  
  #---------------------------------------------------------
end
